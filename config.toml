baseurl = "https://enrdados.netlify.com"
title = "En_R_dados"
languageCode = "es"
defaultContentLanguage = "es"
paginate = "10" # Number of posts per page
theme = "Mainroad"
disqusShortname = "" # Enable comments by entering your Disqus shortname
googleAnalytics = "" # Enable Google Analytics by entering your tracking id
ignoreFiles = ["\\.Rmd$", "\\.Rmarkdown$", "_files$", "_cache$"]

highlightjsCDN = "//cdn.bootcss.com"
highlightjsLang = ["r", "yaml"]
highlightjsTheme = "hybrid"

[Author]
  name = "F.Vilber"
  bio = "F.VilBer true identity is unknown. Maybe he is a successful blogger or writer. Nobody knows it."
  avatar = "img/gritos.png"

[Params]
  subtitle = "Hablamos de R, red y otras cosas..." # Subtitle of your site
  description = "Pagina de programación en R" # Description of your site
  opengraph = true
  twitter_cards = false
  readmore = false # Show "Read more" button in list if true
  authorbox = true
  post_navigation = true
  postSections = ["post"]
  dateformat = "2006-01-02" # change the format of dates
  toc = true
  
[Params.sidebar]
  home = "right"   # Configure layout for home page
  list = "right"   # Configure layout for list pages
  single = "right" # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget (disabled by default)
